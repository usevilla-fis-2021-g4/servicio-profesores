apiVersion: v1
kind: Service
metadata:
  name: api-profesores
  labels:
    {{- include "profesores.labels" . | nindent 4 }}
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  selector:
    app.kubernetes.io/name: api-profesores
    app.kubernetes.io/instance: {{ .Release.Name }}
  type: ClusterIp
  ports:
    - targetPort: 3000
      protocol: TCP
      name: http
      port: 3000